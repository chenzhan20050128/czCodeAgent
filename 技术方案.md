# mca 编程智能体技术方案

> 本文描述计划实现的目标架构，不代表当前完成状态；最终范围以仓库代码、测试和 README 为准。目标是在单机命令行场景中实现一套小而闭合、能解释清楚的 Agent 运行时。

## 1. 设计主张

mca 把模型看成一个不可信但有能力的意图生成器：模型可以建议读文件、改代码或运行命令，却不直接拥有这些权限。本地运行时负责校验、审批、执行、记录和终止。

整个系统围绕一条数据链组织：

~~~text
用户任务
  → 模型产生文本或工具意图
  → 本地状态机接受完整响应
  → 工具管线校验、审批并执行
  → 结果追加为事实事件
  → 从事实投影下一次模型上下文
  → 继续、压缩、完成或恢复
~~~

这条链也是答辩主线：

1. 模型只提议，本地程序掌握执行权。
2. Rollout 是唯一事实源，内存状态和模型 messages 都是它的派生视图。
3. 错误自愈、上下文压缩和会话恢复不是三个外挂功能，而是同一份事实的三种用途。

## 2. 设计取舍

### 2.1 保留的设计不变量

调研 Codex 和 OpenCode 后，本项目只保留六条与单机任务直接相关的不变量：

- Thread/Session、Turn、Sampling Step 分层；一次用户任务通常包含多次模型请求。
- assistant tool call 与对应 tool result 必须组成合法协议闭环。
- provider 的 finish_reason 只是信号，不能单独决定任务完成。
- 模型可见的 messages 是持久事实的规范化投影，不是事实源本身。
- 所有副作用必须先授权、后执行；事后检查不能撤销已经发生的动作。
- 压缩产生新的模型视图，但不能销毁用于审计和恢复的原始事实。

### 2.2 不进入本项目的机制

- 不做 App Server、多前端协议、完整世界快照、OS 沙箱和子 Agent 管理。
- 不做 provider 兼容矩阵、插件、MCP、通用 Permission DSL 和多类专用 Agent。
- 不实现通用 shell AST 安全分类器，也不实现后台进程托管。

这些能力解决生产级、多租户或多前端问题，不属于本项目的单机目标。当前设计优先保证状态机闭合和故障语义清楚。

## 3. 总体架构

~~~text
CLI / REPL
    │
    ▼
SessionController
    ├── RolloutStore      唯一事实源，追加 JSONL
    ├── SessionReducer    事实事件 → 内存运行态
    ├── PromptProjector   运行态 → 合法模型请求
    └── TurnStateMachine  决定采样、执行、压缩和终止
              │
              ├── ModelClient + StreamAssembler
              │
              ├── Compactor
              │
              └── ToolPipeline
                    校验 → 路径解析 → 审批 → 快照 → 执行 → 记录
~~~

### 3.1 组件责任

| 组件 | 只负责什么 | 明确不负责什么 |
|---|---|---|
| SessionController | 串联一次会话，处理 CLI 命令 | 不决定模型协议细节 |
| RolloutStore | 按序、追加、刷盘、加锁、恢复事件 | 不拼模型 messages |
| SessionReducer | 纯函数式地把事件归约成 SessionState | 不写文件、不调模型 |
| PromptProjector | 生成合法、预算内的 Chat Completions 输入 | 不修改事实日志 |
| TurnStateMachine | 唯一决定下一状态 | 不直接执行工具 |
| ModelClient | 完成一次 sampling attempt 和有界 API 重试 | 不拥有 Agent 循环 |
| StreamAssembler | 把 SSE 增量组装成候选响应 | 不执行工具、不直接落事实 |
| ToolPipeline | 对一个工具调用完成全生命周期 | 不决定 Turn 是否结束 |
| Compactor | 生成并验证压缩后的模型视图 | 不删除原始事件 |

这种边界避免出现两个真相源：代码中不存在可独立持久化的 History 对象。SessionState 由 rollout 重放得到；messages 再由 SessionState 投影得到。

## 4. 核心数据模型

### 4.1 标识

- session_id：随机 UUID，只允许规范 UUID 格式，不能直接作为任意文件路径。
- turn_id：一次用户任务的 UUID。
- sample_id：一次完整模型采样的 UUID。
- attempt_id：同一 sample 的一次网络尝试。
- tool_call_id：provider 给出的调用 ID，只在对应 assistant 消息内用于协议配对。
- seq：rollout 内从 1 递增的事件序号。

内部主键不能只依赖 tool_call_id，因为重试后的 provider 可能生成不同 ID，也可能错误复用 ID。

### 4.2 Rollout 事件

每行是一个版本化 JSON 对象，公共字段为 version、seq、event_id、timestamp、session_id、type 和 payload。

最小事件集合：

| 事件 | 关键内容 |
|---|---|
| session_created | cwd、model、上下文窗口、创建时间 |
| turn_started | turn_id、原始用户输入 |
| sampling_failed | sample_id、attempt_id、错误类别；仅诊断，不进入模型历史 |
| assistant_accepted | 完整文本、finish_reason、全部 tool_calls |
| approval_decided | call_id、scope、allow/deny；不记录请求头 |
| tool_started | call_id、工具名、规范化参数摘要 |
| file_snapshot | turn_id、路径、旧内容、mode、existed_before |
| tool_finished | call_id、status、模型可见结果、exit_code、truncated；受管写入另含 after_hash |
| compaction_checkpoint | through_seq、summary、replacement_conversation |
| tool_reconciled | call_id、用户确认的 succeeded/failed/abandoned、说明 |
| undo_finished | turn_id、逐文件结果 |
| turn_finished | completed、failed、interrupted、max_steps_reached |

SSE token、文字 delta 和临时进度只用于 UI，不逐片持久化。只有通过完整性校验的 assistant 响应才成为事实。

### 4.3 派生状态

SessionReducer 重放事件后得到：

- 当前 session 配置和 cwd。
- 已完成及活动 Turn。
- 每个 tool call 的 requested、approved、started、terminal 状态。
- 最新有效 compaction checkpoint。
- 本 Turn 的受管文件基线与 after_hash。
- 是否存在 started 但没有 finished 的不确定副作用。

Reducer 不做 I/O，因此可以用同一组事件做单元测试、恢复和故障注入。

## 5. Turn 状态机

### 5.1 状态

~~~text
IDLE
  → SAMPLING
      → WAITING_APPROVAL → EXECUTING → SAMPLING
      → COMPACTING → SAMPLING
      → FINALIZING
      → COMPLETED
      → FAILED
      → INTERRUPTED
      → MAX_STEPS_REACHED

恢复时发现不确定副作用：RECOVERY_BLOCKED
~~~

每个 session 同时最多有一个活动 Turn。REPL 在 Turn 运行时只响应 Ctrl-C 和审批输入，不支持并发启动第二个 Turn。

### 5.2 正常循环

~~~text
start_turn(user_input):
    append(turn_started)
    tool_steps = 0

    while tool_steps < MAX_STEPS:
        if request_budget_needs_compaction():
            compact_or_fail()

        outcome = sample_once(tools=registered_tools)

        if outcome is complete_text:
            append(assistant_accepted)
            append(turn_finished: completed)
            return

        if outcome is valid_tool_batch:
            append(assistant_accepted)
            execute_and_close_every_call(outcome.tool_calls)
            tool_steps += 1
            continue

        if outcome is context_overflow and not compacted_for_this_step:
            compact_or_fail()
            continue
        if outcome is aborted:
            append(turn_finished: interrupted)
            return
        append(turn_finished: failed)
        return

    final = sample_once(tools=[], tool_choice=none)
    append accepted final text if valid
    append(turn_finished: max_steps_reached)
~~~

MAX_STEPS 统计产生本地工具调用的采样轮数，而不是工具个数。达限后的 FINALIZING 只有一次请求，不暴露任何工具；无论文本是否理想都不再回到循环。若该请求失败，harness 输出确定性的“已达到步骤上限”结果。

每个响应还限制最多 8 个 tool calls。超出的调用不执行，但仍逐个写入 batch_limit_exceeded 结果以闭合协议。网络重试由独立的次数和总时长预算控制，过滤、协议错误和空响应不重试，因此异常分支也不会绕开 MAX_STEPS 形成无限循环。

### 5.3 采样结果分类

ModelClient 不返回模糊的 success，而返回以下之一：

- complete_text：完整终止，文本非空，无工具调用。
- valid_tool_batch：完整终止，至少一个结构合法的工具调用。
- length_exceeded：finish_reason 表明输出截断。
- context_overflow：provider 明确报告上下文超限。
- filtered：内容过滤或拒绝。
- transport_interrupted：缺少合法流终点。
- protocol_error：关键字段缺失或响应结构非法。
- aborted：用户中断。

只有 complete_text 能正常完成 Turn。存在工具调用时，即使 finish_reason 错误地写成 stop，仍按 valid_tool_batch 处理；反过来，没有工具调用也不自动代表完成，空响应、length、filter 和断流都有单独语义。

### 5.4 多工具调用

一条 assistant 消息中的所有 tool_calls 先整体落盘，再按响应顺序执行。参数错误、未知工具、拒绝、超时和执行异常都必须生成一个对应 tool_finished，保证下一次模型请求有且只有一个结果与每个 call 配对。

单个调用失败后仍处理同批剩余调用，因为这些调用由同一次模型响应共同提出；模型在看到完整结果批次后决定下一步。Ctrl-C 是例外：正在运行的调用标为 interrupted，尚未执行的调用标为 not_executed，然后结束 Turn。

## 6. OpenAI 兼容模型协议

### 6.1 支持范围

只支持经过契约测试的 OpenAI Chat Completions 子集：n=1、stream=true、标准 content delta、标准 function tool_calls 和标准错误状态。不同“兼容”网关仍可能有差异，因此不承诺只改环境变量就兼容任何模型。

配置来自 MCA_BASE_URL、MCA_API_KEY、MCA_MODEL 和 MCA_CONTEXT_WINDOW。key 仅进入请求头，不写 rollout 或 verbose 日志。

### 6.2 SSE 解析

SSE 解析器使用增量 UTF-8 解码，识别注释行、空行事件边界、一个事件中的多个 data 行及 data: [DONE]。每个 data JSON 只消费 choices[0]。

工具调用按 tool_call.index 建 accumulator：

~~~text
index → id? + type? + function.name? + arguments_buffer
~~~

id、type、name 和 arguments 在任意 chunk 中都可能缺失；存在才更新。不同 index 可以交错，同一 index 的 arguments 严格按到达顺序拼接，不做不存在的网络乱序重排。

只有同时满足以下条件才接受候选响应：

- 收到合法 finish_reason 和 [DONE]；
- 所有工具调用都有 id、type=function、name 和完整 arguments 字符串；
- finish_reason 不是 length 或 content_filter；
- content 与工具调用至少存在一项。

arguments 的 JSON 语法由 ToolPipeline 校验。若 ID 和工具名完整但 JSON 无效，仍可接受 assistant 并返回 invalid_arguments 工具结果；若 ID 或名称缺失，整次响应是 protocol_error，不能伪造 tool result。

### 6.3 采样重试边界

每次网络尝试使用独立 accumulator。流式 delta 可以实时显示，但在完整响应验收前不写 assistant_accepted，也不启动工具。

- 408、429、部分 5xx、连接和读取错误可按最大次数与总时间预算重试，使用 Retry-After 或指数退避加 jitter。
- 部分流断开时丢弃整个候选，在 UI 标注“本次输出中断并作废”，下一次是重新采样，不与旧文本拼接。
- 400 中明确的 context overflow 只允许触发一次压缩后重试；其他 4xx 直接失败。
- assistant 一旦落盘，或者工具已经开始，就不再以网络错误为由重试该响应或工具批次。
- 有副作用的本地工具从不自动重试。

这个边界保证 API 重试发生在副作用之前，避免把“模型请求重试”误做成“模型加工具事务重放”。

## 7. Prompt 投影与上下文预算

### 7.1 投影规则

PromptProjector 每次从 SessionState 构造请求：

1. 生成一条当前权威 system message：行为约束、cwd、平台、日期、Git 状态和安全边界。
2. 若存在 checkpoint，把 summary 作为明确标记的会话交接上下文加入同一 system message。
3. 从 checkpoint 的 replacement_conversation 开始，追加其 through_seq 之后的新事实；没有 checkpoint 则从会话起点投影。
4. assistant_accepted 投影为 assistant message；每个 terminal tool_finished 投影为对应 role=tool message。
5. 对崩溃遗留状态按第 10 节处理，不能静默伪造成功结果。
6. 最后校验 call ID 唯一、每个调用恰有一个结果、没有孤儿 tool message。

会话处于 RECOVERY_BLOCKED 时禁止生成模型请求。只有 tool_reconciled 为未知调用补出明确的终态结果后，投影才可继续。

工具定义属于当前采样的能力契约，单独放在 tools 字段，不写入对话事实。

### 7.2 预算估算

不依赖特定 tokenizer，因为 OpenAI-compatible 模型可能使用不同分词器。估算完整序列化请求，包括 system、messages、tool schema 和结构开销：

~~~text
estimated_tokens = non_ascii_chars + ceil(ascii_chars / 4) + message_count × 8
available_input = context_window - reserved_output_tokens - safety_margin
~~~

这是偏向提前压缩的启发式，不宣称是精确上界。MCA_CONTEXT_WINDOW 缺失时使用较小的安全默认值；provider usage 仅用于后续显示和校准。

当估算值接近 available_input 时先压缩。即使估算未命中，provider 返回 context overflow 也会触发一次压缩后重试。

### 7.3 结果截断

工具输出同时限制行数与 UTF-8 字节数。超过上限时保留头尾，插入省略行，并在 tool_finished 中写 truncated=true。模型、恢复过程和审计看到的是同一份截断结果；系统不声称保存完整 stdout。

## 8. 压缩检查点

### 8.1 构造流程

1. 只在完整采样边界触发，不能在工具批次执行中间压缩。
2. 从旧投影中选取摘要输入；若太大，先确定性缩短旧 tool output，但保留每条 tool 消息及其 call ID。
3. 使用不暴露工具的专用模型请求生成固定结构摘要：目标、已完成、关键决定、约束、工作区状态、下一步、不确定事项。
4. 选择最近的完整交互组作为 tail。“完整交互组”定义为一条 assistant 消息及其全部 tool results；纯文本 assistant 自成一组。组不得拆分，首部消息与 tail 重叠时按事件序号去重。
5. 生成 replacement_conversation，并做与普通请求相同的协议校验和去重。
6. 将 through_seq、summary 和完整 replacement_conversation 写入 compaction_checkpoint 并刷盘。
7. 只有 checkpoint 落盘成功，Reducer 才切换模型视图；失败时继续使用旧视图。

原始事件永远保留，因此压缩改变的是下一次模型看到什么，不改变“曾经发生过什么”。

### 8.2 恢复方式

恢复时从头重放 rollout，以重建 Turn、工具和 undo 状态；PromptProjector 使用最后一个有效 checkpoint 作为模型对话基线，再投影 through_seq 之后的事件。当前会话规模不需要为了启动速度引入第二套数据库或增量 reducer 快照。

### 8.3 诚实边界

压缩由模型生成，因此有损并可能遗漏细节。保留完整 tail、固定摘要结构、保留原始事实和允许手动 /compact 只能降低风险，不能证明语义无损。多次压缩后准确性下降时应建议开启新会话。

## 9. 工具系统

### 9.1 显式 ToolSpec

六个工具数量固定，直接手写 ToolSpec：name、description、JSON schema、handler、side_effect 和 approval_renderer。schema 与运行时 validator 使用同一份定义，统一要求 type=object、required、基础类型、范围和 additionalProperties=false。

不从 Python 类型和 docstring 自动推导完整 JSON Schema。对六个固定工具而言，显式 schema 更短、更可检查，也避免 Optional、Union 和默认值产生两套语义。

### 9.2 统一执行管线

~~~text
resolve ToolSpec
  → parse JSON arguments
  → validate schema
  → normalize path / command / cwd
  → render exact approval target
  → append approval_decided
  → optional managed-file snapshot
  → append + fsync tool_started
  → execute
  → append + fsync tool_finished
~~~

未知工具、无效 JSON、schema 错误和用户拒绝不会进入副作用步骤，但仍产生可回灌的 terminal tool result。

### 9.3 文件工具

- 所有文件工具只接受相对 session cwd 的路径，绝对路径直接拒绝；解析后用 commonpath 检查规范绝对路径是否仍在 workspace，不能使用字符串 startswith。
- read/list/grep 解析最终目标并确认没有逃出 workspace。
- write/edit 拒绝目标及父路径中的 symlink，并在批准后、写入前再次解析路径和检查旧文件 hash。
- edit 要求 old_text 恰好匹配一次；零次或多次均返回错误。
- write/edit 只处理有大小上限的普通文件。写入同目录临时文件，flush + fsync 后 os.replace，防止 Ctrl-C 留下半文件。
- 这套路径检查降低误操作和普通路径逃逸风险，但不抵御恶意本地进程制造的所有 TOCTOU；它不是 OS 沙箱。

### 9.4 bash

- 使用 Popen，在新的进程组中启动固定 POSIX shell，cwd 固定为 session workspace，stdin=DEVNULL。
- stdout 和 stderr 并发 drain，终端实时显示；回灌模型的内容单独按预算截断，避免 pipe 填满死锁。
- 超时或 Ctrl-C 先发中断/TERM，等待短暂 grace period，再 KILL 整个进程组并 wait，避免遗留孙进程和僵尸进程。
- 记录 exit code、signal、timeout 和 truncated；非零退出码是工具失败事实，不是 Python 异常崩溃。
- 子进程环境默认删除 MCA_API_KEY 等模型凭据。
- bash 每次执行都要批准，不做字符串前缀 always。明显使用 nohup、后台符号或脱离终端的命令给出警告，但本项目不管理其生命周期。

bash 能使用宿主用户的权限并绕过文件工具快照。因此“文件工具限定 workspace”绝不能表述为整个 Agent 已被沙箱隔离。

## 10. 审批、崩溃窗口与恢复

### 10.1 审批语义

文件审批展示规范路径、diff 和批准时的 before_hash；bash 审批展示原始命令、shell 与 cwd。拒绝、审批输入 EOF 或 Ctrl-C 一律 fail closed。

批准后仍要重新检查路径和 before_hash。如果文件已变化，返回 conflict，不执行用户没有看过的新差异。审批决定落 rollout，但 session 恢复后不恢复临时批准状态。

### 10.2 副作用不确定窗口

执行顺序固定为：

~~~text
assistant_accepted 已刷盘
  → tool_started 已刷盘
  → 执行副作用
  → tool_finished 已刷盘
~~~

进程若死在执行和 tool_finished 之间，日志只能证明“执行已经开始”，不能证明成功或失败。恢复后将该调用标为 outcome_unknown，进入 RECOVERY_BLOCKED：

- 不自动重试，不把它伪装成 interrupted 或成功。
- 告诉用户调用内容并要求检查现实状态；阻塞期间禁止再次调用模型。
- CLI 让用户选择“确认成功”“确认失败”或“放弃本 Turn”，并可输入一段说明。系统写入 tool_reconciled：前两种分别投影为 user_confirmed_success / user_confirmed_failure 工具结果，放弃则补 abandoned 结果并结束 Turn。只有该事件刷盘后才可继续。

这不提供 exactly-once，但把无法消除的不确定性变成显式状态，而不是静默重复副作用。

### 10.3 Ctrl-C

- 采样中：关闭 HTTP stream，候选 assistant 作废，写 sampling_failed/turn_finished interrupted。
- 审批中：当前及剩余调用得到 cancelled/not_executed 结果。
- bash 中：按第 9.4 节清理整个进程组；能确认退出时写 interrupted。
- 文件原子替换临界区：完成 replace 后再响应中断，使文件保持旧版或完整新版，不留下半文件。
- 单次 CLI 返回非零退出码；REPL 回到输入提示。

正常 Ctrl-C 应尽力把每个调用写成终态。只有真正崩溃遗留的 started-without-finished 才是 outcome_unknown。

## 11. 文件快照与 undo

每个 Turn 对每个受管文件只保存第一次修改前的基线：path、existed_before、before_bytes、before_mode。每次成功写入更新 after_hash。快照作为 file_snapshot 事件持久化，因此同机 resume 后仍可 undo；文件大小上限同时约束 rollout 膨胀。

执行 /undo 前先对本 Turn 的全部文件做预检：

- 当前 hash 必须仍等于记录的 after_hash，否则报告 conflict，默认不覆盖用户或其他进程的新修改。
- 原文件恢复 before_bytes 和 mode。
- 新建文件仅在内容仍匹配 after_hash 时删除。
- symlink、目录和超出支持大小的文件不进入受管修改。

所有文件通过预检后再执行；若 I/O 中途失败，逐文件报告结果并写 undo_finished(partial)。undo 不能撤销 bash 修改、外部 API、安装软件或后台进程。

## 12. Rollout 物理可靠性

- 会话目录权限 0700，JSONL 文件权限 0600。
- 打开 session 时取得独占锁，防止两个 --resume 同时写。
- 每个事件完整 JSON 序列化后加换行，通过单写入者追加，并在每个事实事件后 flush + fsync；本项目吞吐低，优先简单可靠。
- 追加和刷盘成功后才把事件交给 Reducer；日志写失败时不进入后续副作用。
- 恢复时允许忽略唯一的不完整末行；中间 JSON 损坏、version 不支持、seq 重复或跳跃均停止并报告损坏。
- rollout 保存的是可重建的模型可见结果与执行事实，不保存完整的被截断 stdout，也不会重新执行历史工具。
- verbose 和异常日志永不打印 Authorization；已知 MCA 配置密钥值在写日志前替换为 REDACTED。

append-only 提供审计和状态重建，不提供跨外部系统事务。tool_started 与 tool_finished 之间的未知窗口由第 10.2 节处理。

## 13. 错误策略

| 错误 | 处理 |
|---|---|
| 工具参数、未知工具 | 生成错误 tool result，让模型修正 |
| 工具异常、非零退出、超时 | 生成带类别的 tool result，不自动重试副作用 |
| 用户拒绝 | 生成 denied result，模型可换方案 |
| 429、部分 5xx、连接错误 | 在 assistant 接受前有界重采样 |
| partial SSE | 丢弃候选、提示 UI、整次重采样 |
| context overflow | 压缩后最多重试一次 |
| compaction 失败 | 保留旧视图；仍超限则失败结束 Turn |
| rollout 写失败 | 停止当前动作；若副作用已发生则下次恢复为 unknown |
| 工作区不存在或 resume 配置冲突 | 拒绝静默恢复，要求用户处理 |

错误反馈包含类别、可恢复性和下一步，不输出 traceback 作为普通用户结果；--verbose 可显示经过脱敏的诊断。

## 14. 目录结构

~~~text
mca/
  __main__.py          CLI 与 REPL
  config.py            env、预算和运行配置
  controller.py        SessionController
  state.py             事件、SessionState、Reducer
  rollout.py           JSONL、锁、恢复
  turn.py              TurnStateMachine
  model.py             HTTP、重试、采样结果
  sse.py               SSE 与 tool-call accumulator
  prompt.py            PromptProjector 与预算估算
  compact.py           checkpoint 生成与验证
  approval.py          交互审批
  undo.py              file snapshot 与补偿
  tools/
    registry.py        显式 ToolSpec 与 validator
    filesystem.py      read/list/write/edit
    search.py          grep
    shell.py           bash 与进程组管理
tests/
  fixtures/
demo/
~~~

文件按责任拆分，但不为了“架构感”增加抽象层。Reducer、Projector、SSE assembler 和 Tool validator 都应是可直接单测的纯逻辑；I/O 集中在 store、model 和 handlers。

## 15. 测试方案

### 15.1 确定性单元与故障注入

- SSE：UTF-8 分包、多个 data 行、[DONE]、首 chunk 缺 id/name、不同 index 交错、坏 JSON、半流断开。
- 状态机：stop 与 tool_calls 冲突、空响应、length、filter、MAX_STEPS 后仍企图调用工具。
- 协议闭环：多调用中参数错误、拒绝、超时和 Ctrl-C 后仍有完整 terminal results。
- 重试：partial attempt 不与新 attempt 拼接，assistant 接受后不重采样，工具不自动重试。
- Rollout：尾行截断、中间损坏、重复 seq、双写者、每个 crash 注入点的恢复状态。
- Compaction：tail 不从 tool result 开始、完整 call/result 组、checkpoint 写失败不切换视图。
- 路径：相对路径、绝对路径、..、共同前缀误判、symlink 逃逸、批准后文件变化。
- bash：大量双路输出、超时、孙进程、Ctrl-C、凭据环境清理。
- undo：原文件、新文件、多次修改、外部 hash 变化、部分 I/O 失败、resume 后撤销。

### 15.2 真实模型 E2E

固定模型、参数、fixture commit、工具上限和测试命令，运行一个真实 bug 修复任务至少三次。每次保存：是否得到预期 diff、测试退出码、step 数、重试/压缩次数、最终状态和失败原因。

真实模型 E2E 证明系统能用；fake model 测试证明状态机在边界条件下正确。两者不能互相替代。

## 16. 实现顺序与降级线

1. 先实现事件模型、Reducer 和无工具的单次采样。
2. 实现 SSE assembler、Turn 状态机和 fake model 测试。
3. 接入六个工具及协议闭环。
4. 增加 Rollout 与 --resume，并做 crash window 测试。
5. 增加审批、文件原子写、bash 进程清理和 undo。
6. 最后实现 compaction，不允许它反向侵入工具和状态机。

如果工期受阻，不能删除或弱化题目要求的 Agent 循环、历史管理、工具定义/执行、解析、终止和错误处理。压缩可以先保留手动 /compact 再接自动阈值；undo 可以先限制为受管文本文件；但文档和演示必须如实反映最终实现，不能把计划写成已经完成。

## 17. 答辩讲法

### 17.1 30 秒开场

这个项目的难点不在于让模型生成代码，而在于模型提出工具调用以后，谁控制机器、过程怎样持续、出错或中断后怎样继续。我的方案是从零实现一个单机受控运行时：模型只产生意图，本地状态机完成流式解析、审批、工具执行和终止；每一步写成 append-only 事实，再投影为下一次模型上下文。因此错误分类与反馈、压缩和恢复都建立在同一条事实链上。实现完成后，答辩中的完成态表述必须以实际代码和测试结果为准。

### 17.2 两个最值得展开的决定

第一，为什么完成不能只看 finish_reason：结构上仍有 tool call 就必须执行并回灌；空响应、截断和断流即使没有工具也不是成功。

第二，为什么 rollout 是事实、messages 是投影：同一份事实可以生成合法请求、压缩视图和恢复状态，也能识别副作用结果未知，而不需要维护互相漂移的两份历史。

### 17.3 主动交代边界

- 只支持单机、单 workspace 恢复，不恢复进程。
- 审批和文件快照不是 OS 沙箱，bash 仍有宿主权限。
- 压缩有损，rollout 仍保留原始审计事实。
- OpenAI-compatible 是经过测试的协议子集，不等于兼容所有网关。
- append-only 能暴露未知副作用窗口，不能消除它或保证 exactly-once。

这些边界不是缺陷遮掩，而是本项目为保证每个机制都能实现、测试和解释所做的范围选择。
