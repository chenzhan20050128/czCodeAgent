# mca 编程智能体需求文档

> 文档状态：实现前基线。功能范围在本文冻结；后续只允许修正实现细节和缺陷，不继续扩展能力。

## 1. 项目定位

mca 是一个从零实现的单机命令行编程智能体。用户给出自然语言任务后，它能读取和搜索代码、修改文件、执行命令、观察结果并继续决策，直到完成任务、达到运行边界或需要用户处理不确定状态。

项目要证明的不是“模型会写代码”，而是我能独立实现模型外面的运行时：

> 模型只提出结构化意图；本地运行时负责裁决、执行、记录和终止。每一步被保存成事实，下一次模型上下文、压缩和会话恢复都从同一份事实重建。

## 2. 成功标准

项目成功需要同时满足三项承诺。

### 2.1 能完成真实编程任务

- 支持“搜索代码 → 阅读文件 → 修改文件 → 执行测试 → 根据结果继续”的多步闭环。
- 一次用户任务是一个 Turn；一个 Turn 可以包含多次模型采样和多批工具调用。
- 工具失败、参数错误和用户拒绝不会直接打崩进程，而会作为结果反馈给模型。

### 2.2 执行过程受本地程序控制

- 模型不能直接操作文件或进程，只能返回原生 tool_calls。
- 所有参数先校验；文件修改和 shell 命令在副作用发生前请求批准。
- 文件工具只能操作工作区内的普通文件；路径必须规范化并拒绝符号链接逃逸。
- /undo 可以撤销最近一次 Turn 中经受管文件工具完成的修改，并在检测到外部改动时拒绝覆盖。

### 2.3 过程可记录，会话可恢复

- 已接受的用户消息、模型回复、审批决定、工具开始/结果、压缩检查点和 Turn 终态写入 append-only JSONL。
- 发给模型的 messages 是事实日志的规范化投影，不是另一份独立真相源。
- 上下文接近模型窗口时自动生成压缩检查点；用户也可执行 /compact。
- --resume <session-id> 支持同一机器、同一工作区的会话恢复。
- 如果上次进程退出时某个副作用已经开始但没有结果，系统必须标为 outcome_unknown 并请求用户确认，不能假装未执行或自动重放。

## 3. 题目合规边界

| 题目约束 | 本项目的落实方式 |
|---|---|
| 不使用 agent 框架或 Agent SDK | 只使用普通 HTTP 客户端访问 OpenAI Chat Completions 兼容接口；Agent 循环、状态机、解析器和工具系统全部自行实现 |
| 不使用服务端代码执行或文件工具 | 文件和命令工具全部在本机执行；模型服务只负责生成文本与 tool_calls |
| 自行管理历史与上下文 | 自行实现事实日志、状态归约、消息投影、结果截断和上下文压缩 |
| 自行定义和执行工具 | 手写工具 schema、参数校验、审批和本地执行器 |
| 自行解析模型输出 | 自行解析 SSE，并按 tool-call index 组装流式参数 |
| 自行决定循环终止 | 使用显式 Turn 状态机；不只依赖 provider 的 finish_reason |
| 自行处理错误 | 自行区分采样错误、协议错误、工具错误、中断和不确定副作用 |
| 凭据不得入库或出现在演示中 | API key 仅从环境变量读取；请求头不落日志；子进程默认移除模型凭据 |

## 4. 功能范围

### 4.1 Agent 闭环

1. 接收用户任务并创建 Turn。
2. 从当前会话事实生成合法的模型请求。
3. 流式显示 assistant 文本，并组装完整工具调用。
4. 完整响应通过协议校验后才写入事实日志。
5. 按模型给出的顺序执行工具，并为每个调用写入且仅写入一个结果。
6. 工具结果回灌后再次采样。
7. 只有获得合法的完整文本响应且不存在待执行工具时，Turn 才正常完成。

正常工具采样最多执行 MAX_STEPS 轮。达到上限后只允许一次不暴露工具的收口采样，随后无条件以 max_steps_reached 结束，避免“提示模型停下但仍继续执行工具”的伪上限。

### 4.2 本地工具

固定提供六个工具，不在本项目中增加插件、MCP 或自定义工具市场：

| 工具 | 行为 | 关键约束 |
|---|---|---|
| read_file | 分页读取文本文件并显示行号 | 仅工作区内普通文件 |
| list_dir | 列出目录内容 | 结果有数量上限 |
| grep | 搜索文件内容 | 使用有界流式 rg；未安装 ripgrep 时返回明确错误 |
| write_file | 新建或整体写入文本文件 | 先展示 diff、批准、快照，再原子替换 |
| edit_file | 对唯一匹配的旧文本做替换 | 零匹配或多匹配都拒绝执行 |
| bash | 在工作区执行有超时的 shell 命令 | 每次批准；实时 drain 输出；超时/中断杀进程组 |

所有结果都有字节数和行数上限，保留头尾并明确标注截断。截断用于保护模型上下文，不是保密机制。

### 4.3 审批与补偿

- write_file、edit_file：展示规范路径和 unified diff，用户选择允许一次或拒绝。
- bash：展示准确命令和工作目录，用户选择允许一次或拒绝。
- 本项目不实现通用“永久允许命令”规则；安全解析 shell 需要额外的 AST 和策略系统，不符合当前范围。
- --yolo 只跳过交互审批，不跳过参数校验、文件路径硬边界或凭据清理。它仍会把当前操作系统用户拥有的 shell 权限交给模型，必须在隔离的演示目录中使用。
- /undo 只保证撤销 write_file、edit_file 产生的受管修改，不承诺撤销 bash、后台进程或外部系统副作用。

### 4.4 上下文管理与压缩

- 稳定行为指令、当前工作目录、平台、日期和 Git 状态作为请求上下文提供，不在每个 Turn 重复追加为历史事实。
- 工具 schema 作为每次采样的能力契约发送，不写成聊天消息。
- 请求前校验 assistant tool call 与 tool result 是否完整配对。
- 工具结果先在产生时截断；总请求预算接近上下文窗口时触发压缩。
- 压缩保留最初任务、结构化交接摘要和最近的完整交互组；不得从 assistant/tool 组合中间切断。
- 压缩只替换模型可见视图，不删除原始审计事件；压缩有损，系统必须在 verbose 日志中展示发生时间和范围。

### 4.5 会话持久化与恢复

- 每个会话一个带版本和递增序号的 JSONL rollout。
- 文件默认权限为 0600，会话目录为 0700。
- 每个事实追加成功并刷盘后，才更新内存状态。
- 恢复时允许忽略唯一的、不完整的最后一行；中间损坏、序号重复或跳跃必须停止恢复并报错。
- 同一个 session 同时只能有一个写入者。
- 恢复的是模型上下文和会话配置；文件系统沿用恢复时的真实磁盘状态，不恢复旧进程、后台命令或跨机器执行环境。

### 4.6 CLI

- mca "任务"：运行一次任务后退出。
- mca：进入多轮 REPL。
- mca --resume <session-id>：恢复指定会话。
- mca --verbose：显示采样次数、工具状态、重试、预算和压缩事件。
- mca --yolo：关闭交互审批并打印醒目风险提示。
- REPL 命令：/help、/compact、/undo、/exit。

标准能力只承诺流式展示 assistant content、工具状态和 shell 输出；不承诺所有 provider 都能提供 reasoning 流。

## 5. 明确不做

- 不做 TUI、编辑器插件、远程服务或多前端协议。
- 不做多 Agent、子任务并行、MCP、Skills、插件系统和长期记忆。
- 不做多 provider 兼容矩阵；只支持并测试 OpenAI Chat Completions 兼容协议的有限子集。
- 不做 OS 级沙箱。文件工具的工作区限制、审批和快照只能降低误操作风险；bash 仍继承宿主用户权限。
- 不做后台进程管理和恢复。
- 不保证 exactly-once 副作用；崩溃窗口中的执行结果会显式标为未知。

## 6. 非功能要求

- 可解释：每个关键决定都能用“解决的问题、选择、机制、边界”说明。
- 确定性优先：核心正确性用 fake model / fake SSE 测试，不依赖真实模型偶然表现。
- 有界失败：已分类的连接错误、429/5xx、工具异常、超时和 Ctrl-C 不产生未捕获异常；超过预算后安全结束当前 Turn。
- 最小依赖：Python + 一个 HTTP 客户端；不引入 agent SDK。
- 隐私边界明确：rollout 可能包含代码和命令输出；除保护文件权限和已知凭据脱敏外，不宣称具备通用秘密检测能力。

## 7. 验收标准

### 7.1 确定性测试

以下测试必须全部通过：

- SSE content 与交错 tool-call index 能正确组装；半流断开不会提交半条 assistant。
- 每个 tool call 都得到一个结果，包括参数错误、未知工具、拒绝、超时和中断。
- finish_reason 与结构事实冲突时，状态机不会提前结束。
- 达到 MAX_STEPS 后只发生一次无工具收口采样。
- partial stream 重试不会拼接旧候选，也不会导致工具重复执行。
- JSONL 尾行损坏可恢复，中间损坏、重复 seq 和双写入者会失败关闭。
- 崩溃发生在 tool_started 与 tool_finished 之间时，恢复得到 outcome_unknown 且不自动重试。
- 压缩 tail 不切断 tool-call/result 组，checkpoint 失败不替换旧视图。
- 文件路径 ..、绝对路径和 symlink 逃逸被拒绝。
- undo 能恢复旧文件、删除仍未被外部修改的新文件，并拒绝覆盖 hash 已变化的文件。
- bash 大量输出不会死锁，超时和 Ctrl-C 能清理整个进程组。

### 7.2 真实模型端到端验收

使用固定模型、固定 temperature（若 provider 支持）、固定 MAX_STEPS、固定超时和同一个 Git fixture：

1. fixture 初始测试必然失败。
2. agent 自主读取/搜索并产生预期文件修改。
3. 写入前出现 diff 审批。
4. agent 执行固定测试命令，最终退出码为 0。
5. 退出后用 --resume 恢复，能从事实日志重建目标、已完成工作和工具结果。
6. 同一配置至少运行三次，记录成功次数和失败原因；不以单次成功代替可复现性。

自动压缩单独用长会话 fixture 验证：压缩后仍保留原始目标、关键约束和未完成事项，并保持消息协议合法。

## 8. 开发与提交计划

| 日期 | 交付边界 |
|---|---|
| 8 月 27 日 | 冻结需求/技术方案，建立仓库和测试骨架 |
| 8 月 28 日 | ModelClient、SSE 解析、最小 Agent 循环、六个工具 |
| 8 月 29 日 | rollout、reducer、prompt projection、--resume |
| 8 月 30 日 | 审批、文件原子写、快照/undo、shell 中断 |
| 8 月 31 日 | 自动/手动压缩、故障注入测试、真实模型 E2E |
| 9 月 1 日 | README.txt、视频、公开仓库、凭据和提交历史检查 |
| 9 月 2 日 | 只修阻塞缺陷，完成压缩包和最终提交 |

提交前必须确认：仓库为题目发布后新建的公开仓库；README.txt 不超过 1000 汉字并包含仓库地址、运行方式和特色；视频为不超过 2 分钟、200 MB 的 mp4；以本人姓名命名的 zip 中只包含 README.txt 和视频；截止后不再推送提交；仓库、README、rollout 示例和视频均无凭据。
