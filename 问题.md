# mca 运行问题清单（基于真实大项目跑测）

> 方法：用 `mca --yolo --verbose` 接真实 DeepSeek（deepseek-v4-flash），在隔离目录 `/private/tmp/mca_big2/proj` 让 agent 从零构建一个多文件库 `taskflow`（DAG 调度器 + 单测 + README）。任务一次通过：6 次 `write_file`、4 次 `bash`、2 次 `list_dir`，生成的 19 个单测全绿。随后用 `--resume` 复跑校验会话可重建。
>
> 证据来自该会话 rollout：`44edb061-78ef-4223-8b46-fb54a4a0e202.jsonl`（49 个事件）。

## 结论概览

上一轮已修的 P1/P2/P3 在本轮真实跑测中验证有效：`write_file taskflow/errors.py` 与 `tests/test_scheduler.py` 都自动创建了缺失父目录（`created_directories` 分别记录了 `taskflow/`、`tests/`），模型再没有先跑 `mkdir`；`list_dir {}` 正常列根目录。

本轮新暴露一个**重要**问题（I1），其余为观察项。

---

## I1（重要）：工作区内的绝对路径被一律拒绝，并被错误地记为 `conflict` — 已修复

### 现象

会话第一步，模型读到系统 environment 里的 `cwd=/private/tmp/mca_big2/proj`，很自然地调用：

```
seq=3 list_dir {"path": "/private/tmp/mca_big2/proj"}
seq=5 tool_finished status=conflict
       result="tool execution conflict: PathSafetyError: absolute paths are not allowed"
```

一次工具调用被浪费，且模型收到的状态是 `conflict`。

### 两个耦合缺陷

1. **可用性**：`WorkspaceResolver._relative_path` 对任何绝对路径直接抛 `PathSafetyError`，即使这个绝对路径就落在工作区内。但运行时又主动把绝对 `cwd` 写进 environment 发给模型，等于“给了它一个地址，又拒绝它按这个地址寻址”。相对路径与工作区内绝对路径本应是同一份文件的两种拼写，应当都接受。

2. **语义**：读类工具（read_file / list_dir / grep）是 `handler`，没有 prepare 阶段，路径错误发生在 `tool_started` 之后，被 executor 的 `except (FileConflictError, PathSafetyError)` 统一归为 `conflict`。而 `conflict` 在本系统里有专门含义——它绑定 undo / TOCTOU（“文件在批准与提交之间被外部改动”）。把一个**确定性的坏参数**标成 `conflict`，既误导模型，也污染了 conflict 的语义。

### 修复方向（非补丁式）

- 统一路径策略：`_relative_path` 接受“工作区内的绝对路径”，规范化为工作区相对路径；对逃逸工作区的绝对路径、`..` 逃逸、NUL、Windows 盘符绝对路径仍然拒绝。真正的包含性防护（`_require_inside` + `resolve(strict)` + 写路径逐段拒绝符号链接）保持不变，只是多接受一种等价拼写。
- 澄清失败分类：只有带副作用的 prepared 工具在 execute 期才可能命中真正的 TOCTOU `conflict`；纯读 handler 抛 `PathSafetyError` 是一次失败的读，应记 `failed`，不再冒用 `conflict`。

### 落地（已完成）

- `WorkspaceResolver._relative_path` 新增 `_relativize_absolute`：用 `os.path.realpath` 把绝对路径规范化后再 `relative_to(workspace)`，工作区内则转成相对路径，逃逸则抛 `absolute path escapes workspace`；盘符/UNC 绝对路径直接拒绝。`resolve_read`/`resolve_write` 后续的包含性与符号链接检查完全复用。
- `ToolExecutor.execute` 按 `spec.prepare_handler is not None` 区分：带 prepare 的写工具在 execute 期抛 `PathSafetyError/FileConflictError` 记 `conflict`（真 TOCTOU），纯读 handler 记 `failed`。
- 覆盖测试：`test_resolver_accepts_a_workspace_internal_absolute_path`、`test_resolver_accepts_the_workspace_root_as_an_absolute_path`、`test_write_accepts_a_workspace_internal_absolute_path`、`test_list_dir_accepts_a_workspace_internal_absolute_path`、`test_grep_accepts_a_workspace_internal_absolute_path`、`test_read_tool_path_safety_error_is_failed_not_conflict`，并把逃逸用例扩到 `/etc`、盘符路径。全量 372 tests 通过。

---

## 观察项（非缺陷，记录备查）

- **O1 — `--resume` 非交互默认拒绝**：resume 复跑时没带 `--yolo`，stdin 非交互，审批读到 EOF → 默认 `deny`，模型如实回复“无法确认测试通过”。这是 fail-closed 的正确行为，非缺陷；如需无人值守续跑应显式加 `--yolo`。
- **O2 — 自动压缩未被触发**：单个任务上下文远小于窗口，压缩逻辑没进入。压缩仍由 `tests/test_compact.py` 的确定性用例覆盖；真实长会话触发留作后续验证。
